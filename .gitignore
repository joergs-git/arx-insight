# never commit private training data or secrets
*.fdb
*.FDB4
config.json
arx_report_data.json
arx_report.html
.venv/
goals.json
__pycache__/
.DS_Store
.effort_cache.json
firebird/
CLAUDE.md
tasks/
# local copy of the ARX app dump / database data - must never be published
ARX/
.ai_cache.json
# runtime state of the local app (pid, port, shutdown secret)
instance.json
# plan ledger (per-person recommendations) - private like goals.json
plans.json
.detail_cache.json
